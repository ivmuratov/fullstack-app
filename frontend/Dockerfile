# скачать из репозитория Docker
# и установить NodeJS, причём, самую лёгкую 
# (все самые лёгкие версии популярных фреймворков
# и библиотек в докере принято называть alpine).
FROM node:alpine 

# задаём рабочую директорию, с которой будем работать
WORKDIR /app/front

# открываем порт 3000. Дальнейшая связь с приложением, 
# запущенным в контейнере, будет происходить через этот порт.
EXPOSE 3000

# копируем содержимое исходного проекта в рабочую папку контейнера.
COPY ./ ./

# устанавливаем все пакеты, необходимые для запуска приложения.
RUN npm install

# запускаем приложение командой ~ npm start.
CMD ["npm", "start"]